{
  "name": "kora-backend",
  "version": "1.0.0",
  "description": "Secure backend proxy for Kora app OpenAI API calls",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}